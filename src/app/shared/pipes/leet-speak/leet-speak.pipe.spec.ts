import { LeetSpeakPipe } from './leet-speak.pipe';

describe('LeetSpeakPipe', () => {
  it('create an instance', () => {
    const pipe = new LeetSpeakPipe();
    expect(pipe).toBeTruthy();
  });
});
