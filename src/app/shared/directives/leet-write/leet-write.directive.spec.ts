import { LeetWriteDirective } from './leet-write.directive';

describe('LeetWriteDirective', () => {
  it('should create an instance', () => {
    const directive = new LeetWriteDirective();
    expect(directive).toBeTruthy();
  });
});
